name: My First Pipeline Test

on:
  push:
    branches: [ main ]

jobs:
  stage-1-build:
    runs-on: ubuntu-latest
    steps:
      - name: Simulate downloading code
        run: echo "✅ Code downloaded successfully"

      - name: Simulate installing dependencies
        run: echo "✅ All dependencies installed"

  stage-2-test:
    needs: stage-1-build
    runs-on: ubuntu-latest
    steps:
      - name: Run fake test 1
        run: echo "✅ Login test passed"

      - name: Run fake test 2 - THIS WILL FAIL
        run: exit 1        # ← THIS LINE causes the failure

      - name: Run fake test 3
        run: echo "✅ This will be SKIPPED because test 2 failed"

  stage-3-deploy:
    needs: stage-2-test
    runs-on: ubuntu-latest
    steps:
      - name: Deploy app
        run: echo "🚀 This entire job will be SKIPPED too"
